// 4- Make a program that, given a value defined in a variable, returns “positive” if that value is positive, 
//“negative” if it is negative and “zero” otherwise.

const a = 0;

if (a > 0){ 
    console.log("positive");
}
else if (a < 0) {
    console.log("negative");
}
else {
    console.log("zero");
}
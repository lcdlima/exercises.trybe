// 8- Write a program that defines three numbers in variables at the beginning and returns true if one of the three is even. 
//Otherwise, it returns false.

// Bonus: use only an if.

const a = 3;
const b = 6;
const c = 7;

if (a%2==0 || b%2==0 || c%2==0) {
    console.log(true);
}
else {
    console.log(false);
}
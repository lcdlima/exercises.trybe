// 2- Make a program that returns the greater of two numbers. 
//Define two variables at the beginning of the program with the values to be compared.

const a = 6;
const b = 1; 

if (a >= b) {
    console.log(a);
} else {
    console.log(b);
}